
; This applies to the Spring Breeze pause screen
; Make it so holding another button (probably Select) brings up the actual pause screen
; WRAM $000500 Pause screen palette data
; WRAM $000100 is also pause screen data?
; WRAM $06000 is the pause menu text
; WRAM $07000 is where corkboard graphics are kept
; Tileset pause screen starts at VRAM $7800

; $00165ED4
; $0016B57E